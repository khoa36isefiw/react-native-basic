import { StyleSheet } from "react-native";

export const OPENSANS_REGULAR = "OpenSans-Regular";

// use for the whole app
export const globalStyles = StyleSheet.create({
    // use this fonts for the app
    globalFont:{
        fontFamily: OPENSANS_REGULAR,
    }
})